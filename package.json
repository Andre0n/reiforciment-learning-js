{
  "name": "reinforcement-js",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=dist/index.js",
    "start": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=dist/index.js --watch",
    "dev": "python -m http.server 8080 --directory dist -C -1"
  },
  "keywords": [],
  "author": "André Gabriel",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "chart.js": "^4.4.3"
  }
}
